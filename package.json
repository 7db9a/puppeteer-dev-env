{
  "name": "7db9a-puppeteer",
  "author": "7db9a",
  "private": true,
  "version": "0.1.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "mocha",
    "precommit": "lint-staged"
  },
  "lint-staged": {
    "{src,test}/**/*.js": [
      "prettier --print-width 100 --single-quote --trailing-comma all --no-semi --write",
      "git add"
    ]
  },
  "license": "MIT",
  "dependencies": {
    "del": "^3.0.0",
    "puppeteer": "^1.20.0",
    "sharp": "^0.21.1"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "lint-staged": "^7.3.0",
    "mocha": "^5.2.0",
    "prettier": "^1.14.3"
  }
}
