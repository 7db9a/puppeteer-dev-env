version: "3"

services:
  renderer:
    image: puppeteer:latest
    ports:
      - 8080:3000
    expose:
      - "3000"
    user: root
    working_dir: /app
    command: bash -c "npm install --quiet && npm run start"
    volumes:
      - ./:/app
volumes:
 puppeteer-renderer-data-volume:
